---
import { translations } from '../../i18n/astro';

interface Props {
  locale?: string;
}
const { locale = 'en' } = Astro.props;
const tMap: Record<string, any> = translations; const t = tMap[locale] || translations.en;
---

<section id="faq" class="relative w-full py-24 sm:py-32 reveal">
  <div class="max-w-3xl mx-auto px-4 sm:px-6">
    <div class="mb-12 text-center reveal">
      <span class="text-[11px] font-mono text-obsidian-purple tracking-[0.15em] uppercase">{t.faq.label}</span>
      <h2 class="text-[clamp(1.6rem,4vw,2.8rem)] font-semibold mt-3 mb-3 text-obsidian-heading reveal" data-delay="1">{t.faq.title}</h2>
      <p class="text-obsidian-muted text-sm max-w-lg mx-auto">{t.faq.subtitle}</p>
    </div>

    <div class="flex flex-col gap-3">
      {t.faq.items.map((item: { q: string; a: string }, idx: number) => (
        <details class="group rounded-xl card card-hover reveal" data-delay={idx * 2}>
          <summary class="flex items-center justify-between p-5 cursor-pointer select-none list-none">
            <span class="text-sm font-medium text-[#e5e5e5] pr-4">{item.q}</span>
            <svg class="w-4 h-4 text-obsidian-dim flex-shrink-0 transition-transform duration-200 group-open:rotate-180" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg>
          </summary>
          <div class="px-5 pb-5 pt-0 border-t border-obsidian-border/50">
            <p class="text-sm text-obsidian-muted leading-relaxed" set:html={item.a}></p>
          </div>
        </details>
      ))}
    </div>
  </div>
</section>